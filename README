This Repository is the default starter project that sets up all of the annoying things you'd normally have to do at the start of a project.

It sets up the directory structure and adds the default index, css and js files.